# Deployment

## Frontend

The frontend will be deployed on Vercel.

## Backend

The backend will be deployed to:

- Railway as the primary API server
- Render as the failover API server

## Database

The PostgreSQL database will be hosted on Neon.

## Environment Variables

Each deployment environment must define its own environment variables.

Sensitive values must never be committed to GitHub.